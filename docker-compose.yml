version: '3.8'

services:
  search-bot:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=True
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
    volumes:
      - .:/app
    command: python search_bot.py
